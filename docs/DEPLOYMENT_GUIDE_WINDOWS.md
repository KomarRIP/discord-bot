# План деплоя проекта в режиме разработки на Windows 11

**Дата создания:** 2024-12-20  
**Целевая ОС:** Windows 11  
**Режим:** Разработка (Development)

---

## 📋 Содержание

1. [Требования к системе](#1-требования-к-системе)
2. [Установка необходимого ПО](#2-установка-необходимого-по)
3. [Получение токена Discord бота](#3-получение-токена-discord-бота)
4. [Клонирование и настройка проекта](#4-клонирование-и-настройка-проекта)
5. [Настройка переменных окружения](#5-настройка-переменных-окружения)
6. [Установка зависимостей](#6-установка-зависимостей)
7. [Проверка базы данных и миграций](#7-проверка-базы-данных-и-миграций)
8. [Запуск проекта](#8-запуск-проекта)
9. [Настройка Discord сервера](#9-настройка-discord-сервера)
10. [Проверка работоспособности](#10-проверка-работоспособности)
11. [Решение проблем](#11-решение-проблем)

---

## 1. Требования к системе

### Минимальные требования:
- **ОС:** Windows 11
- **Node.js:** версия 18.x или выше (рекомендуется 20.x LTS)
- **npm:** версия 9.x или выше (идет в комплекте с Node.js)
- **Свободное место на диске:** минимум 200 МБ
- **RAM:** минимум 512 МБ свободной памяти
- **Интернет:** стабильное подключение для установки зависимостей и работы с Discord API

---

## 2. Установка необходимого ПО

### 2.1. Установка Node.js

1. Откройте браузер и перейдите на [nodejs.org](https://nodejs.org/)
2. Скачайте **LTS версию** (рекомендуется 20.x или новее)
3. Запустите установщик `.msi`
4. Следуйте инструкциям мастера установки:
   - Оставьте все опции по умолчанию
   - Убедитесь, что выбрана опция "Add to PATH"
5. После установки проверьте версию в PowerShell:

```powershell
node --version
npm --version
```

**Ожидаемый результат:**
```
v20.x.x
10.x.x
```

### 2.2. Установка Git (опционально, если проект клонируется)

Если проект находится в Git-репозитории:
1. Скачайте Git для Windows с [git-scm.com](https://git-scm.com/download/win)
2. Установите с настройками по умолчанию
3. Проверьте установку:

```powershell
git --version
```

---

## 3. Получение токена Discord бота

### 3.1. Создание приложения Discord

1. Откройте [Discord Developer Portal](https://discord.com/developers/applications)
2. Войдите в свой аккаунт Discord
3. Нажмите **"New Application"**
4. Введите название приложения (например, "DS Bot")
5. Подтвердите создание

### 3.2. Настройка бота

1. В левом меню выберите **"Bot"**
2. Нажмите **"Add Bot"** и подтвердите
3. В разделе **"Token"** нажмите **"Reset Token"** и скопируйте токен
   - ⚠️ **ВАЖНО:** Сохраните токен в безопасном месте, он больше не будет показан
4. Включите следующие опции:
   - ✅ **Public Bot** (если нужен публичный доступ) или оставьте выключенным для приватного использования
   - ✅ **Message Content Intent** (если планируется чтение сообщений)
5. В разделе **"Privileged Gateway Intents"** включите:
   - ✅ **SERVER MEMBERS INTENT** (требуется для работы с ролями участников)
   - ✅ **MESSAGE CONTENT INTENT** (если планируется чтение сообщений)

### 3.3. Настройка OAuth2

1. В левом меню выберите **"OAuth2" → "URL Generator"**
2. В разделе **"Scopes"** выберите:
   - ✅ `bot`
   - ✅ `applications.commands`
3. В разделе **"Bot Permissions"** выберите следующие права:
   - ✅ **Manage Roles** (обязательно)
   - ✅ **Manage Channels** (обязательно)
   - ✅ **View Channels** (обязательно)
   - ✅ **Send Messages**
   - ✅ **Embed Links**
   - ✅ **Read Message History**
   - ✅ **Manage Messages** (опционально)
4. Скопируйте сгенерированную URL внизу страницы
5. Откройте эту URL в браузере и пригласите бота на тестовый сервер

### 3.4. Важные замечания по правам

⚠️ **КРИТИЧНО:**
- Роль бота в Discord сервере должна быть **выше всех ролей**, которыми он будет управлять
- Переместите роль бота в настройках сервера наверх в списке ролей
- Без этого бот не сможет выдавать/управлять ролями

---

## 4. Клонирование и настройка проекта

### 4.1. Открытие проекта

Если проект уже находится на вашем компьютере:
1. Откройте PowerShell или Command Prompt
2. Перейдите в директорию проекта:

```powershell
cd "C:\Users\User\Desktop\Арма\ds bot"
```

Если проект нужно клонировать:
```powershell
git clone <repository-url>
cd "ds bot"
```

### 4.2. Проверка структуры проекта

Убедитесь, что в директории проекта присутствуют следующие файлы и папки:
- `package.json` - файл зависимостей
- `tsconfig.json` - конфигурация TypeScript
- `src/` - исходный код
- `config/` - конфигурационные файлы
- `templates/` - шаблоны Discord серверов
- `migrations/` - миграции базы данных

---

## 5. Настройка переменных окружения

### 5.1. Создание файла .env

1. В корневой директории проекта создайте файл `.env`
   - В Windows можно использовать команду:

```powershell
New-Item -Path .env -ItemType File
```

2. Откройте файл `.env` в текстовом редакторе (Notepad, VS Code, и т.д.)

### 5.2. Заполнение переменных окружения

Добавьте в файл `.env` следующие переменные:

```env
# Обязательные переменные
DISCORD_TOKEN=ваш_токен_бота_здесь
DATABASE_URL=./data/bot.db

# Опциональные переменные (можно оставить значения по умолчанию)
NODE_ENV=development
LOG_LEVEL=info

# Опционально: путь к конфигурационному файлу
# RUNTIME_CONFIG_PATH=./config/runtime.json

# Опционально: директория с миграциями
# MIGRATIONS_DIR=./migrations

# Опционально: ID Discord сервера для локальной разработки (guild-specific commands)
# DISCORD_GUILD_ID=ваш_guild_id_здесь
```

**Важно:**
- Замените `ваш_токен_бота_здесь` на токен, полученный в разделе 3
- Путь к базе данных указан как `./data/bot.db` (относительно корня проекта)
- База данных будет создана автоматически при первом запуске

### 5.3. Создание директории для базы данных

Создайте директорию для базы данных:

```powershell
New-Item -Path data -ItemType Directory -Force
```

### 5.4. Безопасность .env файла

⚠️ **ВАЖНО:** Убедитесь, что файл `.env` добавлен в `.gitignore` (если используете Git):

```powershell
# Проверьте наличие .gitignore
Get-Content .gitignore

# Если .env отсутствует, добавьте:
Add-Content .gitignore "`n.env"
```

---

## 6. Установка зависимостей

### 6.1. Установка npm пакетов

В PowerShell выполните команду:

```powershell
npm install
```

Это установит все зависимости, указанные в `package.json`:
- `discord.js` - библиотека для работы с Discord API
- `better-sqlite3` - драйвер для SQLite базы данных
- `typescript` - компилятор TypeScript
- `tsx` - утилита для запуска TypeScript в режиме разработки
- `dotenv` - загрузка переменных окружения
- `pino` - система логирования
- И другие зависимости

**Ожидаемое время установки:** 1-3 минуты

### 6.2. Проверка установки

После установки проверьте, что все зависимости установлены:

```powershell
npm list --depth=0
```

Должны быть установлены все пакеты без ошибок.

---

## 7. Проверка базы данных и миграций

### 7.1. Проверка миграций

Убедитесь, что файлы миграций присутствуют:

```powershell
Get-ChildItem migrations\*.sql
```

Должен присутствовать файл `0001_initial.sql` или аналогичные.

### 7.2. База данных создается автоматически

База данных будет создана автоматически при первом запуске приложения с применением всех миграций. Ручное создание не требуется.

---

## 8. Запуск проекта

### 8.1. Запуск в режиме разработки (рекомендуется)

Для разработки используйте режим с автоматической перезагрузкой:

```powershell
npm run dev
```

Эта команда:
- Запускает TypeScript код напрямую через `tsx`
- Автоматически перезагружает приложение при изменении файлов
- Показывает логи в консоли

**Ожидаемый вывод:**
```
[timestamp] INFO: Bot ready
[timestamp] INFO: Slash commands registered
```

### 8.2. Альтернативный способ: компиляция и запуск

Если хотите сначала скомпилировать проект:

```powershell
# Компиляция TypeScript в JavaScript
npm run build

# Запуск скомпилированного кода
npm start
```

### 8.3. Проверка запуска

После запуска в консоли должны появиться логи:
- ✅ "Bot ready" - бот подключился к Discord
- ✅ "Slash commands registered" - команды зарегистрированы
- ❌ Любые ошибки будут отображены с подробностями

### 8.4. Остановка приложения

Для остановки нажмите `Ctrl+C` в терминале.

---

## 9. Настройка Discord сервера

### 9.1. Приглашение бота на сервер

Если вы еще не пригласили бота:
1. Используйте URL, сгенерированную в разделе 3.3
2. Или создайте новую URL в Discord Developer Portal:
   - OAuth2 → URL Generator
   - Выберите права и скопируйте URL
   - Откройте URL в браузере и выберите сервер

### 9.2. Настройка роли бота

⚠️ **КРИТИЧНО:**
1. Откройте настройки сервера → **Роли**
2. Найдите роль вашего бота
3. **Переместите роль бота на самый верх** списка ролей (выше всех ролей, которыми бот будет управлять)
4. Убедитесь, что у роли включены необходимые права (они должны быть включены автоматически при приглашении)

### 9.3. Проверка прав бота

Убедитесь, что у бота есть следующие права:
- ✅ Управление ролями
- ✅ Управление каналами
- ✅ Просмотр каналов
- ✅ Отправка сообщений
- ✅ Встраивание ссылок

---

## 10. Проверка работоспособности

### 10.1. Проверка команд в Discord

1. Откройте Discord и перейдите на сервер, где установлен бот
2. В любом канале введите `/` и начните вводить команду
3. Должны появиться команды бота, например:
   - `/setup` - мастер настройки
   - `/deploy` - деплой структуры
   - `/intake` - работа с заявками
   - `/roles` - управление ролями

### 10.2. Первичный запуск Setup Wizard

1. Выполните команду `/setup start` в Discord
2. Следуйте инструкциям мастера настройки
3. Выберите шаблон `SSO_RF`
4. Заполните параметры:
   - Имя подразделения
   - Размер подразделения
   - Режим приема
5. После завершения мастера можно применить деплой командой `/deploy apply`

### 10.3. Проверка логов

В консоли приложения должны отображаться логи всех операций:
- Подключение к Discord
- Регистрация команд
- Обработка интеракций
- Ошибки (если есть)

---

## 11. Решение проблем

### 11.1. Ошибка "Missing env var DISCORD_TOKEN"

**Причина:** Не установлена переменная окружения `DISCORD_TOKEN`

**Решение:**
1. Проверьте наличие файла `.env` в корне проекта
2. Убедитесь, что в файле есть строка: `DISCORD_TOKEN=ваш_токен`
3. Проверьте, что нет лишних пробелов или кавычек вокруг токена

### 11.2. Ошибка "Cannot find module"

**Причина:** Зависимости не установлены или установлены некорректно

**Решение:**
```powershell
# Удалите node_modules и package-lock.json
Remove-Item -Recurse -Force node_modules
Remove-Item -Force package-lock.json

# Переустановите зависимости
npm install
```

### 11.2.1. Ошибка "better_sqlite3.node is not a valid Win32 application"

**Причина:** Нативный модуль `better-sqlite3` был скомпилирован для другой архитектуры или версии Node.js

**Решение 1: Переустановка зависимостей (рекомендуется)**

```powershell
# Удалите node_modules и переустановите
Remove-Item -Recurse -Force node_modules
Remove-Item -Force package-lock.json
npm install
```

**Решение 2: Использование предскомпилированных бинарников**

Если переустановка не помогла, можно попробовать использовать предскомпилированные бинарники:

```powershell
npm install better-sqlite3 --build-from-source=false
```

**Решение 3: Установка инструментов сборки (если нужна компиляция)**

Если требуется компиляция нативного модуля, установите Visual Studio Build Tools:

1. Скачайте **Visual Studio Build Tools** с [visualstudio.microsoft.com/downloads](https://visualstudio.microsoft.com/downloads/)
2. Установите с компонентом **"Desktop development with C++"**
3. После установки переустановите зависимости:

```powershell
Remove-Item -Recurse -Force node_modules
npm install
```

**Примечание:** Обычно переустановка зависимостей решает проблему, так как npm попытается использовать предскомпилированные бинарники для вашей платформы.

### 11.3. Ошибка "Error: ENOENT: no such file or directory" для базы данных

**Причина:** Директория для базы данных не создана

**Решение:**
```powershell
New-Item -Path data -ItemType Directory -Force
```

### 11.3.1. Ошибка "ENOENT: no such file or directory, scandir '...\templates'"

**Причина:** Неправильный путь к директории шаблонов в конфигурации

**Решение:**
Проверьте файл `config/runtime.json` - путь к шаблонам должен быть `"../templates"` (относительно директории `config/`):

```json
{
  "templatesPath": "../templates",
  ...
}
```

Убедитесь, что директория `templates` существует в корне проекта и содержит файлы шаблонов (например, `SSO_RF.json`).

### 11.4. Бот не отвечает на команды

**Возможные причины:**
1. Бот не запущен - проверьте консоль приложения
2. Команды не зарегистрированы - подождите 1-2 минуты после запуска
3. Неправильный токен - проверьте токен в `.env`
4. Отсутствуют необходимые Intents - включите `SERVER MEMBERS INTENT` в Developer Portal

**Решение:**
1. Перезапустите приложение: `Ctrl+C`, затем `npm run dev`
2. Проверьте логи в консоли на наличие ошибок
3. Проверьте настройки Intents в Discord Developer Portal

### 11.5. Ошибка "Missing Access" или "Forbidden" при деплое

**Причина:** У бота недостаточно прав или роль бота слишком низкая

**Решение:**
1. Убедитесь, что роль бота находится **выше всех управляемых ролей**
2. Проверьте права роли бота в настройках сервера
3. Убедитесь, что бот имеет права "Manage Roles" и "Manage Channels"

### 11.6. Ошибка компиляции TypeScript

**Причина:** Несоответствие версий или ошибки в коде

**Решение:**
```powershell
# Проверьте версию TypeScript
npm list typescript

# Выполните проверку типов
npm run typecheck

# Проверьте ошибки линтера
npm run lint
```

### 11.7. База данных заблокирована

**Причина:** Другой процесс использует базу данных

**Решение:**
1. Закройте все запущенные экземпляры приложения (`Ctrl+C`)
2. Подождите несколько секунд
3. Запустите приложение снова

### 11.8. Проблемы с путями в Windows

Если возникают проблемы с путями (особенно с кириллицей в пути):

**Решение:**
1. Используйте абсолютные пути в `.env`:
   ```env
   DATABASE_URL=C:\Users\User\Desktop\Арма\ds bot\data\bot.db
   ```
2. Или переместите проект в путь без кириллицы, например: `C:\projects\ds-bot`

---

## 12. Дополнительная информация

### 12.1. Структура проекта

```
ds bot/
├── src/                    # Исходный код TypeScript
│   ├── app/               # Application layer (use-cases)
│   ├── config/            # Конфигурация
│   ├── domain/            # Domain layer (entities, policies)
│   ├── infra/             # Infrastructure layer
│   ├── interface/         # Interface layer (Discord commands)
│   └── shared/            # Общие утилиты
├── dist/                  # Скомпилированный JavaScript (генерируется)
├── config/                # Конфигурационные файлы
│   └── runtime.json       # Runtime конфигурация
├── templates/             # Шаблоны Discord серверов
│   └── SSO_RF.json       # Шаблон ССО РФ
├── migrations/            # SQL миграции базы данных
├── data/                  # База данных (создается автоматически)
├── .env                   # Переменные окружения (создать вручную)
├── package.json           # Зависимости проекта
├── tsconfig.json          # Конфигурация TypeScript
└── README.md              # Основная документация
```

### 12.2. Полезные команды

```powershell
# Разработка (с автоперезагрузкой)
npm run dev

# Компиляция TypeScript
npm run build

# Запуск скомпилированного кода
npm start

# Проверка типов без компиляции
npm run typecheck

# Проверка кода линтером
npm run lint
```

### 12.3. Режимы работы

- **Development (`npm run dev`)**: Автоперезагрузка, подробные логи, отладка
- **Production (`npm start`)**: Оптимизированный режим, скомпилированный код

### 12.4. Логирование

Логи отображаются в консоли с использованием библиотеки Pino:
- `INFO` - информационные сообщения
- `WARN` - предупреждения
- `ERROR` - ошибки

Уровень логирования можно настроить через переменную `LOG_LEVEL` в `.env`:
- `debug` - все логи
- `info` - информационные и выше (по умолчанию)
- `warn` - только предупреждения и ошибки
- `error` - только ошибки

---

## 13. Следующие шаги после деплоя

После успешного запуска проекта:

1. **Изучите документацию:**
   - `docs/core.md` - архитектура проекта
   - `docs/current-status-and-plan.md` - текущий статус и план
   - `docs/Command-guide.md` - руководство по командам

2. **Проверьте шаблоны:**
   - Изучите `templates/SSO_RF.json` для понимания структуры

3. **Настройте сервер:**
   - Запустите `/setup start` в Discord
   - Следуйте мастеру настройки
   - Примените деплой командой `/deploy apply`

4. **Разработка:**
   - Используйте `npm run dev` для разработки с автоперезагрузкой
   - Файлы из `src/` автоматически компилируются при изменении

---

## 📞 Поддержка

При возникновении проблем:
1. Проверьте раздел [Решение проблем](#11-решение-проблем)
2. Изучите логи в консоли приложения
3. Проверьте документацию в папке `docs/`

---

**Успешного деплоя! 🚀**

